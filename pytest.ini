[pytest]
asyncio_mode = auto
markers =
    integration: mark tests as integration (external services)
    selenium: mark UI tests that require Selenium
    playwright: mark UI tests that require Playwright
